# External registry coverage — Edge package

Production project (deployment approval required): `azdmuarzntzqdyirysux`

After migration `202608200004` is applied and verified, deploy only:

1. `external-prospect-discovery`

The executable adapter list contains the verified France and Norway adapters and the verified, bounded Poland `KRS:` identifier lookup. Germany, Italy, Spain, Netherlands, and Belgium are coverage metadata and normalization fixtures only: none has a request builder in the executable adapter list, and each has a zero runtime request cap.

The package retains the exact source paths needed by the function import graph, its Deno regressions, the bounded source-QA script, and the legal/source review. It contains no provider credential and requires no traffic-analytics, frontend, or cPanel deployment.

Do not enable a blocked provider, add a paid dependency, call AI, send email/notifications, or deploy another Edge Function as part of this package.
