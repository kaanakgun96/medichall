# Tender Search Restored v1

Fixes:
- restores a separate Tender Search tab,
- lists all open tenders independently of opportunity matches,
- keeps AI-matched Opportunities as a separate tab,
- allows document discovery and Claude analysis before a match exists,
- moves homepage search queries to Tender Search,
- fixes the archive-preparation recursion bug,
- refreshes tender cards after discovery and analysis.

No SQL or new Edge Function is required for this UI hotfix.
Upload `cpanel/portal.html` as the live `portal.html`.
